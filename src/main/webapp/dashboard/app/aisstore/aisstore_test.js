'use strict';

describe('dashboardApp.aisstore module', function() {

  beforeEach(module('dashboardApp.aisstore'));

  describe('aisstore controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('AisStoreCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});